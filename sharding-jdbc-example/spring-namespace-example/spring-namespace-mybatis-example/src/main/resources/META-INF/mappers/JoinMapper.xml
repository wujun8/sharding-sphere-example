<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.shardingsphere.example.spring.namespace.mybatis.fixtrue.repository.JoinRepository">

    <resultMap id="order" type="io.shardingsphere.example.spring.namespace.mybatis.fixtrue.entity.Order">
        <id column="order_id" property="orderId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
    </resultMap>
    
    <select id="selectJoin"  resultMap="order">
        SELECT o.*
        FROM t_order o
        LEFT JOIN t_order_item i ON o.order_id=i.order_id
        LIMIT 1000
    </select>

    <select id="selectJoinByUser"  resultMap="order">
        SELECT o.*
        FROM t_order O
            LEFT JOIN t_order_item i ON o.order_id=i.order_id
        WHERE o.user_id=#{user_id,jdbcType=INTEGER}
    </select>

</mapper>
