<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.shardingsphere.example.spring.namespace.mybatis.fixtrue.repository.PageRepository">

    <resultMap id="order" type="io.shardingsphere.example.spring.namespace.mybatis.fixtrue.entity.Order">
        <id column="order_id" property="orderId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
    </resultMap>
    
    <select id="selectOrderPage"  resultMap="order">
        SELECT
            o.*
        FROM
            t_order o
        ORDER BY
            o.order_id
        LIMIT
            #{limitStart,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
    </select>

</mapper>
